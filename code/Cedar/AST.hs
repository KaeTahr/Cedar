module Cedar.AST where

-- data Program = Program [ExternalDeclaration]
--   deriving (Show, Eq)

-- data ExternalDeclaration
--   = FunctionDef FunctionDef
--   | Declaration Declaration
--   deriving (Show, Eq)

-- data FunctionDef = FunctionDefNode
--   { specifiers :: [DeclarationSpecifier]
--   , declarator :: Declarator
--   , declarations :: Maybe [Declaration]
--   , body :: CompoundStatement
--   } deriving (Show, Eq)

-- data Declaration = DeclarationNode
--   { specifiers' :: [DeclarationSpecifier]
--   , initDeclarators :: Maybe [InitDeclarator]
--   } deriving (Show, Eq)

-- -- Define other AST nodes: Declarator, DeclarationSpecifier, InitDeclarator, Statement, Expression, etc.

-- -- Example:
-- data Expression
--   = IntLiteral Int
--   | FloatLiteral Double
--   | Variable String
--   | BinaryOp String Expression Expression
--   -- ... other expressions
--   deriving (Show, Eq)
